#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
int main(void)
{
	int n1, n2, n3, min;
	scanf("%d", &n1);
	scanf("%d", &n2);
	scanf("%d", &n3);
	min = n1;
	if (n2 < n1)
		min = n2;
	if (n3 < n2)
		min = n3;
	printf("最小的整数是%d。\n", min);
		
		
		
		return 0;
